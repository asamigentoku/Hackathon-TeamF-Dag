
                int searchType = scanner.nextInt();
                scanner.nextLine();

                if(searchType == 1){

                    System.out.print("検索する授業名を入力してください: ");
                    String keyword = scanner.nextLine();

                    boolean found = false;
